<div class="component-blog-teaser">
    <p> {{ top_title }}</p>
    <a href="{{ top_link_url }}">{{ top_link_text }}</a>
    <div class="blog-container">
      <div class="col-wrapper">
        <div class="greentext">{{ article }}</div>
        <h2 class="darkpurpletext">{{ title }} </h2>
        <div>
            {{ description1 }}
            <a href="{{ inside_link_url }}">{{ inside_link_text }}</a>
            {{ description2 }}
        </div>
        <a href="{{ link_url }}" class="button">{{ link_text }}</a>
      </div>
    {{ image|raw }}
    </div>
</div>